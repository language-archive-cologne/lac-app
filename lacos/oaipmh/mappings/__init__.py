"""Placeholders for BLAM → metadata mappings.

Actual field mappings will be provided later. For now, the module simply defines
constants referencing the supported prefixes so other modules can import from a
single place.
"""

OAI_DC_PREFIX = "oai_dc"
OLAC_PREFIX = "olac"
SCHEMA_ORG_PREFIX = "schema_org"
